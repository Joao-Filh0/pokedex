import 'package:flutter/material.dart';

class ErrorHomeComponent extends StatelessWidget {
  const ErrorHomeComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
